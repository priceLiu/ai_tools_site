---
description: 标签 / 工具 / 分类关系与开发流程；开发前须通读 tool-tag-taxonomy-counting.md（计数与四者关系）并对照本规则
alwaysApply: true
---

# 标签 · 工具 · 分类 — 关系口径与开发流程

凡改动 **标签（tags）、工具（tools）、左侧产品线分类（categories）、场景（tag_categories）、角色（role_categories）、导航菜单、首页/分类列表**，或新增依赖上述数据的特性时，**必须先完成下列流程，再写代码**。

> **硬性要求**：涉及 **`tool_tags`**、**场景/角色收录与计数**、**挂载或移除挂载**、**`tags.tag_category_id` / `role_category_tags`**、或 **`neonCountPublicListedTools*`** 同类逻辑的改动，开发前 **必须完整仔细阅读** [`docs/tool-tag-taxonomy-counting.md`](docs/tool-tag-taxonomy-counting.md)（含置顶「⭐ 重要说明」与各节对照），**不得**仅凭本节摘要开工。

## 一、强制流程（顺序不可省略）

1. **读本规则全文**（本节 + 第二节关系表 + 第三节自检清单）。
2. **通读计数与关系权威文档**（与代码冲突时以迁移与 `lib/neon/data.ts` 为准，并应顺手修正文档）：
   - **[`docs/tool-tag-taxonomy-counting.md`](docs/tool-tag-taxonomy-counting.md)** — **开发前必读**：工具 / 标签 / 场景分类 / 角色的联结、**前台与后台计数口径**、挂载 vs 关联 vs 移除、选人 SQL 与计数差异；**须从头到尾读过**，尤其对「⭐ 重要说明」与 §5 易混项。
   - [`docs/tag-taxonomy-admin-alignment.md`](docs/tag-taxonomy-admin-alignment.md) — 四条轴线、后台能力、历史决策。
   - [`docs/label.md`](docs/label.md) — 场景 / 标签 / 角色与 URL。
3. **用一两句话写清**：本次改动触碰哪几条「轴线」、是否改变前台列表/计数口径、是否需要缓存与 `revalidate`。
4. **输出实施方案**（要点清单：涉及表、查询入口、`revalidatePath`/`revalidateTag`、后台入口），**待用户确认或自检无害后再改代码**。
5. **实施**：只做方案范围内的最小改动；若有 schema 变更必须新增迁移文件并在 [`docs/done.md`](docs/done.md)（按项目规则）记录。

## 二、关系口径（当前实现 — 必须遵守）

### 2.1 三条独立概念

| 概念 | 含义 | 勿混淆 |
|------|------|--------|
| **标签（tags）** | 全局词表；工具通过 **tool_tags** 挂多个标签（上限见代码）。 | 不是菜单分类的别名。 |
| **工具（tools）** | 一条业务实体；审核状态、隐藏 `is_disabled`、首页热门 **`is_featured`**。 | 热度热门与「菜单类 hot」.slug 联动的是 **精选字段**，列表数据源见代码。 |
| **菜单产品线（categories）** | 左侧导航 / `/category/[slug]` 用的分类树；可有 **`is_disabled`**。 | 与 **tag_categories（场景）** 是不同表。 |

### 2.2 工具 ↔ 菜单分类（产品线）

- **`tools.category_id`**：**主分类**（表单、卡片展示、与 categories 外键）；写入路径应保证与 **`tool_categories`** 一致策略见 `lib/neon/data.ts`（如 `neonEnsureToolMenuCategoryLink`）。
- **`tool_categories (tool_id, category_id)`**：**多对多**：前台分类 subtree 下列表以 junction **为主**（见 `neonListToolsForCategoryIds`）；一条工具可挂在多条菜单分类下。
- **`category_tags (category_id, tag_id)`**：仅表达「运营上该菜单分类关联哪些标签」；**不负责** `/category` 下工具列表收录逻辑（与工具挂载正交）。

### 2.3 工具 ↔ 标签

- **`tool_tags`**：工具 ↔ 标签 M:N，排序 `sort_order`。
- **场景**：`tags.tag_category_id` → `tag_categories`；聚合页 `/tag-category`、`/tag`。
- **角色**：`role_categories` + **`role_category_tags`** → 再落到带标签的工具集合。
- **`tags.is_disabled`**：前台展示标签与依赖标签的聚合时需过滤（具体见现有 Neon 查询）。

### 2.4 「热门工具」特例

- 首页 `#home-hot` 与 **已通过且未隐藏** 条件下的精选列表由 **`tools.is_featured`** 驱动。
- `slug === 'hot'` 的 **categories** 行与 junction 可能不完全一致；后台「菜单分类管理」里热门 Tab 的数据与操作路径已与精选语义对齐（见 `adminAddToolToHotFeaturedAction` 等）。**新功能勿假设「热门 = 仅 junction」或「仅 category_id」。**

### 2.5 计数与统计

- **前台可见**：通常约束 **`status = approved`**、**`is_disabled = false`**，菜单侧 junction 边需 **`categories.is_disabled = false`**（与柱状图、后台 Tab 数字对齐时应沿用同一套 JOIN，避免两套口径）。
- **后台列表**：可按需求展示「含隐藏」的挂载以便清理；**Tab 上展示给运营的「前台条数」**不得把已隐藏工具算入（除非需求明确要求「库内全量」并标注）。

## 三、自检清单（动手前快速打勾）

- [ ] **已通读** [`docs/tool-tag-taxonomy-counting.md`](docs/tool-tag-taxonomy-counting.md)（凡触碰工具↔标签↔场景/角色或计数者 **必选**）。
- [ ] 本次改动是否修改「用户在 `/category` 能看到哪些工具」？若是，是否只动 **`tool_categories` / 查询**，还是误用了 **`category_tags`**？
- [ ] 是否影响首页 bundle、ISR、`HOME_TOOL_BUNDLE_CACHE_TAG` 或快照？是否需要 **`revalidateHomeToolBundleAction`** 同类失效？
- [ ] 是否新增「第四套轴线」或把两套表合成一套查询？— **禁止无声合并**，须在方案里写明语义。
- [ ] 工具详情 / 提交表单 / 批量导入是否同步 junction 与 **`category_id`**？

## 四、文档维护

若代码层面更改了上述任一关系（例如列表改为并集策略、热门改为纯 junction）、或变更 **场景/角色收录计数 JOIN**，须同步更新 **`docs/tool-tag-taxonomy-counting.md`**（计数与关系口径）、**`docs/tag-taxonomy-admin-alignment.md`** 或 **`docs/label.md`** 相应段落，避免后续 agent 误读旧段落。
